#include <stdio.h>
/**
 * main - prints the last digit of a number.
 * Return: 0
 */
int print_last_digit(int)
{
	int n;
	if (n % 10 > 0)
		putchar(n % 10 + '0');

		return (1);
	else
		return (0);
}

